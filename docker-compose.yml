services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      - PORT=7010
    ports:
      - "7010:7010"

  web:
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - "7011:7011"
    volumes:
      - .:/usr/share/nginx/html:ro
    command: >
      /bin/sh -c "sed -i 's/listen  *80;/listen 7011;/' /etc/nginx/conf.d/default.conf
      && exec nginx -g 'daemon off;'"
